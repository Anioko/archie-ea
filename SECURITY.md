# Security

- This repository contains **no** production data, credentials, or secrets.
- Set strong, unique values for every variable in `.env` before any deployment.
- Rotate any credential that may ever have been shared or committed elsewhere.
- Report vulnerabilities privately to the maintainers (add a contact here).
