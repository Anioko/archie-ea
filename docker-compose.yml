version: '3.4'
services:
  server:
    build:
      context: .
    # Bring the schema up before starting the web server (all idempotent, non-destructive):
    #   init-db          — create any missing tables (create_all). Without it, gunicorn
    #                      boots against an empty DB and every request 500s ("relation does not exist").
    #   reconcile-schema — add any columns the models gained since this DB was created.
    #                      create_all never alters existing tables, so a long-lived DB drifts
    #                      and core pages 500 (e.g. arb_review_items.jira_issue_key). See
    #                      docs/known-issues/schema-drift-on-existing-databases.md
    #   backfill-principle-org — Principle gained TenantMixin; reconcile-schema adds
    #                      organization_id as NULL, and the tenant filter compares with
    #                      `=`, so NULL matches nothing. Without this every pre-existing
    #                      architecture principle silently disappears from the governance
    #                      dashboard, the ARB screens and the AI chat tools. Non-fatal:
    #                      it refuses to guess when several organisations exist.
    #   backfill-archimate-layer-casing — archimate_elements.layer accumulated two
    #                      casings and Postgres `=` is case-sensitive, so the column
    #                      partitioned itself. ArchiMateElement.layer now canonicalises
    #                      both stored values and query literals to lower case, which
    #                      means a legacy capitalised row matches NOTHING until this
    #                      has run. It is therefore required, not optional, on any
    #                      database that predates that change. Idempotent.
    #   backfill-architect-role — normalise existing users onto the architect role.
    command: sh -c "flask --app manage init-db && flask --app manage reconcile-schema && { flask --app manage backfill-archimate-layer-casing || echo 'WARN archimate layer casing backfill skipped - ArchiMate elements stored with a capitalised layer will not match any query until it runs'; } && { flask --app manage backfill-value-stream-tenancy || echo 'WARN value-stream tenancy backfill skipped - run manually with --org-id'; } && { flask --app manage backfill-principle-org || echo 'WARN principle tenancy backfill skipped - run manually with --org-id'; } && { flask --app manage backfill-initiative-org || echo 'WARN initiative tenancy backfill skipped - run manually with --org-id'; } && { flask --app manage backfill-kanban-card-org || echo 'WARN kanban card tenancy backfill skipped'; } && flask --app manage backfill-architect-role && gunicorn -c gunicorn.conf.py manage:app"
    # Guardrails added 2026-07-30. Previously restart=no and no mem_limit, so a
    # memory spike triggered a GLOBAL OOM (22 Jul) and a wedged container stayed
    # wedged for six days (24-30 Jul). The ceiling confines a spike to this
    # container; unless-stopped brings it back by itself.
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"
    mem_limit: 2600m
    memswap_limit: 2600m   # == mem_limit: no swap allowance
    ports:
      # NOT 0.0.0.0. Publishing on all interfaces left the app reachable from the
      # open internet on :5000, bypassing Caddy entirely - no TLS, no request
      # timeouts, none of the security headers (verified 2026-07-30: arbitrary
      # scanner IPs held established connections straight to gunicorn).
      #   127.0.0.1  - host-side healthcheck and the archie-watchdog timer
      #   10.106.0.6 - this droplet's DigitalOcean VPC address, which is where
      #                the Caddy droplet (10.106.0.2) now proxies to. If the
      #                droplet's private IP ever changes, update both this and
      #                the proxy's Caddyfile.
      - '127.0.0.1:5000:5000'
      - '10.106.0.6:5000:5000'
    volumes:
      - './:/app'
    environment:
      # Database URLs - separate databases for dev and test
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/archie
      DEV_DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/archie
      TEST_DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/archie_test
      REDISTOGO_URL: redis://redis:6379
      GUNICORN_WORKERS: "4"
      # 4x8. Measured on this 2 vCPU box at concurrency 32: 4 workers gave
      # 89 req/s p95 628ms vs 2 workers 51 req/s p95 1588ms. Threads are the
      # cheap axis (~0 MB each); each extra worker costs ~355 MB.
      GUNICORN_THREADS: "8"
      GUNICORN_MAX_REQUESTS: "1000"
      # 8+4 per process x 4 workers = 48 connections, under max_connections=100.
      SQLALCHEMY_POOL_SIZE: "8"
      SQLALCHEMY_MAX_OVERFLOW: "4"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf --max-time 10 http://127.0.0.1:5000/health || exit 1"]
      interval: 30s
      timeout: 12s
      retries: 3
      start_period: 420s
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  worker:
    # Disabled 2026-07-30. Its only job is sending email, but no MAIL_/SMTP
    # settings exist anywhere, so it could not send - it only logged messages,
    # while holding ~1 GB (25% of RAM). Parked behind a profile, not deleted:
    #   re-enable with:  docker compose --profile email up -d worker
    profiles: ['email']
    build:
      dockerfile: Dockerfile.worker
      context: .
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"
    mem_limit: 1200m
    memswap_limit: 1200m   # == mem_limit: no swap allowance
    volumes:
      - './:/app'
    environment:
      # Database URLs - separate databases for dev and test
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/archie
      DEV_DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/archie
      TEST_DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/archie_test
      REDISTOGO_URL: redis://redis:6379
      # The RQ worker opens its own pool too; it only sends email, so keep it small.
      SQLALCHEMY_POOL_SIZE: "4"
      SQLALCHEMY_MAX_OVERFLOW: "2"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  postgres:
    image: pgvector/pgvector:pg15
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"
    mem_limit: 448m
    memswap_limit: 448m   # == mem_limit: no swap allowance
    environment:
      POSTGRES_USER: postgres
      # Sourced from .env, which is gitignored. It must NOT be hardcoded here:
      # docker-compose.yml is tracked and this repository is published.
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: postgres
    volumes:
      - db-data:/var/lib/postgresql/data
      # Init script creates flask_dev and flask_test databases on startup
      - ./scripts/database/init-databases.sh:/docker-entrypoint-initdb.d/init-databases.sh:ro
    ports:
      # Bind to loopback only — the app reaches Postgres over the internal Docker
      # network (host 'postgres'), so this never needs to be public. Publishing on
      # 0.0.0.0 exposed the DB to the internet (Shadowserver/DO security finding).
      - '127.0.0.1:5432:5432'
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  adminer:
    image: adminer
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"
    mem_limit: 64m
    memswap_limit: 64m   # == mem_limit: no swap allowance
    ports:
      # Adminer is a DB admin UI — loopback only (reach it via an SSH tunnel), never public.
      - '127.0.0.1:8080:8080'
    depends_on:
      - postgres

  redis:
    image: redis:6-alpine
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"
    mem_limit: 160m
    memswap_limit: 160m   # == mem_limit: no swap allowance
    # noeviction is deliberate: Redis is the RQ broker, so evicting keys would
    # silently drop queued jobs. maxmemory is a guard rail, not a cache policy.
    # Do NOT add --save here: any --save argument overrides the compiled-in
    # defaults and yielded an EMPTY save policy (verified), silently disabling
    # RDB snapshots. Omitting it keeps the default 3600/1, 300/100, 60/10000.
    command: redis-server --maxmemory 128mb --maxmemory-policy noeviction
    ports:
      # Loopback only — the app uses Redis over the Docker network (host 'redis').
      # Publishing on 0.0.0.0 left Redis open to the internet on 6379 (the security finding).
      - '127.0.0.1:6379:6379'

volumes:
  db-data:
