# Testing requirements for Playwright Python
pytest-playwright>=0.4.0
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-bdd>=6.0.0
playwright>=1.40.0
coverage>=7.0.0
# Add to requirements-test.txt for performance testing
psutil>=5.9.0
requests>=2.31.0

# Moved out of requirements.txt: test tooling was shipping in the production
# image, enlarging its attack surface and its CVE reporting surface for no
# runtime benefit. CI installs this file explicitly.
pytest>=9.0.3                # CVE: 1 advisory at 8.3.3
# pytest-asyncio removed. Its 0.x line is incompatible with the pytest 9 pinned
# above: pytest_collectstart does `collector.obj` on a Package collector, which
# pytest 9 does not expose, and the resulting INTERNALERROR aborts COLLECTION -
# so the CI Tests job ran zero tests while reporting a failure that looked like
# a broken suite. There are no async tests here to justify carrying it.
# Reinstate as pytest-asyncio>=1.0 (with asyncio_mode in pytest.ini) if async
# tests are added.
pytest-playwright>=0.7.0,<0.8.0
pytest-json-report>=1.5.0,<2.0.0
pytest-timeout>=2.2.0,<3.0.0
pytest-xdist==3.6.1
pytest-mock>=3.11.0,<4.0.0
pytest-rerunfailures>=13.0,<15.0
pytest-cov>=5.0.0,<7.0.0
playwright>=1.47.0,<2.0.0
