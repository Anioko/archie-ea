.webassets-cache/
webassets-external/
.DS_Store
*.pyc
*.pyo
env
venv
*.env
.env.*
env*
dist
*.egg
*.egg-info
# No SQLite files - using CockroachDB only
.idea
dump.rdb
ssl/
.vscode/
.history/
node_modules/
package-lock.json
app/static/styles/output.css

# Tailwind CSS standalone CLI binary (download from GitHub releases)
# Downloaded tool binaries. Never commit one: they are platform-specific, large,
# and a committed binary is the one file nobody re-reads before trusting.
scripts/bin/

# Tailwind CSS built output. The compiled tailwind-output.css IS committed so a
# fresh clone / `docker compose up` renders styled without a Node toolchain or a
# build step (the Docker image is Python-only). Regenerate it with the build
# script when templates/classes change; the hashed variants stay ignored.
app/static/css/tailwind-output.*.css
# --check writes a pid-suffixed backup and restores from it. If the
# process dies mid-check the file survives; it is a build artifact,
# never a source file.
app/static/css/*.checkbak*

# Asset manifest (build artifact from scripts/build_css.py)
app/static/manifest.json

# Test execution tracking (local working file, not for repo)
.test_run_log.json

# Agentic governance — local session state (never committed)
.active_task.json
.wip_meta.json
.pre_task_scan.json

# Clutter & AI Temporary Files
00_*.txt
000_*.txt
BASELINE_*.txt
BASELINE_*.md
PHASE1_*.txt
PHASE1_*.md
FINAL_*.txt
COMPREHENSIVE_*.txt
COMPREHENSIVE_*.md
COMPLETE_*.txt
DELIVERABLE_*.txt
DOCUMENTATION_*.txt
EXPECTED_*.txt
FILES_CREATED_*.txt
FILES_CREATED_*.md
MODEL_*.txt
PACKAGE_*.txt
PROJECT_*.txt
QUICK_*.txt
READY_*.txt
TEST_*.txt
VALIDATION_*.txt
VALIDATION_*.md
VENDOR_*.txt
# ...but the vendored-asset provenance record is a tracked artefact, not clutter.
# It is how "what is in app/static/vendor and where did it come from" is answered
# for an enterprise security review, and the vendor-integrity gate reads it.
# Second time this "one-off looking name" pattern has hidden a permanent file —
# see the scripts/check_*.py note further down.
!app/static/vendor/VENDOR_MANIFEST.txt
VENDOR_*.md
📖_*.txt
🎉_*.txt
🎊_*.txt
⚡_*.txt
⭐_*.txt
START_*.txt
IMMEDIATE_*.txt
verification_output*.txt
reference_table_html.txt
nul

# Screenshots (except in designated folder)
*.png
!readme_media/*.png
!docs/images/*.png

# Root-level temporary scripts
/check_*.py
/debug_*.py
/test_*.py
/run_*.py
/verify_*.py
/fix_*.py
/execute_*.py
/run_*.bat
/setup_*.bat
/install_*.bat

# Local dev/diagnostic scripts (do not push to server)
/_*.py
/_*.txt
/_*.json

# Generated scan artifact (contains local machine paths)
docs/duplicate_routes_static_scan.json

# Runtime user uploads — real tenant data, must NEVER be version-controlled.
# (Open-source readiness Phase 1/2: isolate Example Corp data.) Working copies
# on disk and on the production server survive untracking because the deploy
# uses `git reset --hard` with no `git clean`.
app/uploads/
uploads/

# Stray test/result/audit output dumps (dev artifacts, not app files)
absolute_final.txt
all_tests_results.txt
completion.txt
journey_test_results.txt
pom_retest_results.txt
solution_*_pom_final.txt
pushlog.txt

# Generated BA review quality report (output of check_ba_review_quality.py)
docs/ba_review_quality.json

# Databases
*.db
*.sqlite
data-*.sqlite

# Log files
*.log

# Flask instance folder
instance/

# Test results and reports
test-results/

# Discovery pipeline output (regenerated on demand)
discovery_report/
tests/comprehensive/results/
*_TEST_REPORT*.md
TEST_*.md

# Python test coverage artifacts
.coverage
coverage.xml
htmlcov/

# Root-level utility scripts
/init_*.py
logs/staged_diff.patch

# Temporary analysis/artifact directories
artifacts/
backups/

# One-time analysis scripts and outputs
/inspect_*.py
scripts/fix_*.py
scripts/prune/db_cleanup*.py
scripts/prune/execute_db_cleanup.py
scripts/prune/db_analysis.json
scripts/prune/*.txt
.aider*

.claude/
.cursor/

verification_artifacts/

# Screenshot evidence files (captured during E2E verification sessions)
*.jpg
*.png
!docs/design_system/*.png
!app/static/img/*.png
!app/static/img/*.jpg

# One-off diagnostic scripts.
#
# NOTE: these globs are why DESIGN.md can reference a `check_token_migration.py`
# pre-commit hook that is absent from the repository — a permanent quality gate was
# named like a throwaway and got ignored out of existence. Permanent gates must be
# negated below, not just named carefully.
scripts/check_*.py
# Permanent gates — must stay tracked despite the check_*.py glob above.
# Note: gitignore has no inline comments, so each negation gets its own line.
!scripts/check_design_tokens.py
!scripts/check_external_origins.py
!scripts/check_sri.py
!scripts/check_deployed_deps.py
!scripts/check_raw_sql_tenancy.py
!scripts/check_fabricated_data.py
!scripts/check_macro_import_context.py
!scripts/check_dead_interactions.py
!scripts/check_broken_surfaces.py
!scripts/check_template_syntax.py
!scripts/check_silent_data.py
!scripts/check_error_signalling.py
!scripts/check_fetch_guards.py
!scripts/check_null_filters.py
!scripts/check_template_references.py
!scripts/check_template_calls.py
!scripts/check_tenant_unique.py
scripts/patch_*.py
scripts/find_*.py
scripts/delete_*.py
scripts/remove_*.py
scripts/search_*.py
scripts/diagnose_*.py
scripts/verify_*.py
scripts/final_verify.py
scripts/deploy_and_restart.py
scripts/test_jwt_codegen.py
scripts/add_jira_project_model.py

# Superpowers plugin cache
.superpowers/

# Production pull working copy
prod_pull/

# Journey test report files (generated by E2E runs, not source artifacts)
docs/journey_reports/*-journey-results.txt
docs/journey_reports/*-journey-results.json

# Generated artifacts
*.zip
tmp_*.js
business_rules/

# E2E test environment
.env.e2e

offsite_backups/

generated_demo_mobile/
generated_paper_demo/
.tmp_lucid_fix/

# --- open-source safety (appended) ---
.env
.env.*
!.env.example
*.sql
*.sqlite
*.dump
*.pem
*.key
cookies*.txt
uploads/
instance/
*.backup
*.bak

# Static-analysis and coverage artefacts (added 2026-07-30)
# SonarQube scanner working dir (huge, vendored licence files exceed Windows path limits)
.scannerwork/
# junit report - generated
test-results.xml

# --- Local scratch / working files ---------------------------------------------
# Ad-hoc scripts and dumps that accumulate at the repo root during investigation.
# Listed explicitly rather than with broad globs (build_*.py, seed_*.py) so a
# legitimately-named future module is not silently untracked.
#
# CI scans the FULL git history with gitleaks, so an accidental `git add -A` that
# commits a dump containing real data is expensive to undo. Prefer putting new
# throwaway work in scratch/ below.
scratch/
b_gz.html
b_plain.html
h_gz.txt
h_plain.txt
build_diagram.py
build_engagement.py
build_phase5.py
codegen_incontainer.py
seed_caps_overlap.py
seed_sap_sfdc.py

# Ad-hoc REPL scripts that reached the repo root and were committed by accident
# (qc.py, si.py, readgen.py, recheck.py — 11-19 lines each, one with a hardcoded
# solution_id=11, referenced by nothing). Deleted 2026-08-03. Named explicitly
# rather than by glob so a legitimately-named future module is not swallowed.
qc.py
si.py
readgen.py
recheck.py
.worktrees/
