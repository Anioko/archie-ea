# Bounded implementation assignment F500-061

Produce a proposed patch for the two supplied files only. Do not run commands, install packages, commit, deploy, access credentials, or expand scope. Coordinator will apply, test and independently verify browser outcomes. Stop after proposing the patch and listing remaining gaps.

Production evidence: populated Architecture CSV export crashes because canonical ArchiMateElement has archimate_type, not element_type. The service aliases that model as ArchitectureElement but reads element.element_type and uses element_type= in both import constructors. Preserve existing CSV header element_type for compatibility while mapping to canonical archimate_type internally. JSON export uses model.to_dict(); canonical JSON uses archimate_type, and legacy input may use element_type. Reject missing/conflicting type inputs honestly rather than silently fabricating a type. Do not claim complete relationship round-trip support: relationship import is absent and outside this bounded patch.

The new test file currently fails before reaching the real defect due to incomplete SQLAlchemy mapper configuration. Replace that artificial hydration with a focused query-boundary row double that deliberately exposes only canonical fields. Clearly document its scope. Add CSV export-content and import-constructor regressions with isolated query/session doubles. Preserve tenant-aware ORM querying; never disable tenant hooks or add a global model alias to hide the bug. Tests must not access a database. Do not add SQLite fixtures. Coordinator separately owns shared PostgreSQL-fixture integration and browser download tests.

Keep the patch small. No unrelated formatting/refactoring. Return precise tests to run, not a claim that you executed them. The application route's content type, download cleanup timing, authorization, data validation and broader relationship-import contracts remain coordinator follow-up; list any observed concerns.
