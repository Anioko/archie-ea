# F500-062 governance editor repair

Implement a bounded repair for four existing blueprint controls: + Exception, + Compliance, + Change, + Review. Live /solutions/32 + Exception did nothing. _governance_compliance.html calls openEntityModal; blueprint.js sets activeModal but no entity editor exists in blueprint.html. Do not claim the whole generic entity editor family is fixed; other types remain follow-up.

Own only app/static/js/solutions/blueprint.js, app/templates/solutions/blueprint.html, a new app/templates/solutions/partials/_blueprint_governance_editor.html, tests/test_blueprint_governance_editor.py, and docs/qa/claude-governance-result.md. No other edits; Codex owns shared ledger, CSS rebuild, integration, full-app smoke tests, commit/push/deploy and production testing. Do not spawn agents, install packages, access credentials/production or run the full suite.

Read DESIGN.md fully before edits and use the existing components/modal.html macro + Platform.modal lifecycle, not another local x-show modal. Existing Inter, neutral cards, semantic colors, h-9 buttons, labelled inputs, no coloured card edges/corners, no raw Tailwind palette. Reuse application/user/element pickers where entity fields require them; do not turn entity references into free-text fields. Don't change permissions, invent defaults/data, hide controls, or replace working flow with navigation elsewhere.

Trace actual governance API routes/models and JS submitEntity/defaults/list handling before implementing. Read errors and return envelopes rather than assuming save succeeded. Editors must open on first click, show correct existing field values for edit, validate required fields, save through existing authorized endpoint, retain input and show error on failure, refresh correct list on success, close/cancel safely, and support keyboard dismissal/focus. Do not implement dangerous delete actions as part of this task. Use same modal for all four with clear titles and type-specific fields; keep other entity types untouched.

Write reproducing Chromium tests using the real partial, modal infrastructure and blueprint handler with only network boundaries doubled. Test open/cancel for all four, payload/endpoint, success refresh, and failed save preserving editor/values. State precisely what the tests cover and do not cover. No DB fixture fallback; full DB/browser is coordinator-owned. Run only your focused test file, report red/green evidence and remaining gaps in the result file. Stop when ready for review; do not commit or deploy. If command denied, report it honestly.
