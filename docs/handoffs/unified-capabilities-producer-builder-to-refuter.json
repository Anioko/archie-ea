{
  "from": "builder",
  "to": "refuter",
  "artifact": "docs/buckets/unified-capabilities-producer/tasks/01-run-the-producer.result.md, docs/buckets/unified-capabilities-producer/tasks/02-write-time-sync.result.md, docs/buckets/unified-capabilities-producer/tasks/03-register-the-gate-and-demonstrate.result.md",
  "approval_status": "approved",
  "gate_conditions": [
    "store_agreement_gate_registered_and_passing",
    "461_rows_projected_with_provenance",
    "write_time_sync_uses_shared_sql",
    "browser_verified_non_empty"
  ],
  "unmet_conditions": [],
  "timestamp": "2026-09-17T06:00:00Z",
  "notes": "Round 3. Refuter's round 2 review confirmed D1/D2/D4/D6/D9 genuinely fixed and found 5 remaining issues (R2-1 HIGH blocker, R2-2 HIGH, R2-3/R2-4/R2-5 MEDIUM). All 5 fixed this round. (1) R2-1 (the headline fix): app/commands/project_capabilities.py's blocker 2 (owner_or_code_changed_since_projection) is no longer a whole-batch-aborting hard blocker -- _plan() now tags every blocker dict 'blocking': True/False (only blocker 2 is False) and collects the actual moved business_capability ids ('source_ids'). run_projection() raises ProjectionBlocked only for hard_blockers; the moved rows' ids are excluded (via a new 'excluded_ids' bind threaded through _SOURCE_CTE, _PARENT_SQL, _BACKLINK_SQL and _verify()) from this run's writes and reported in a new report['skipped'] key (visible in both --dry-run and --apply, echoed by the CLI). Every other row -- including brand-new never-projected rows -- still projects normally in the same run instead of the whole database going stale on one moved row. app/models/business_capabilities.py's write-time listener updated to pass excluded_ids explicitly (mechanical, no behaviour change -- it only ever narrows to single_id already). scripts/database/deploy-schema.sh's WARN message corrected to stop naming owner_or_code_changed_since_projection as a whole-run-blocking reason, since it no longer is one. (2) R2-2: added test_moved_row_is_skipped_not_blocking_the_whole_run and test_moved_row_does_not_block_a_dry_run_either to tests/test_capability_projection.py (cloned-schema, exercise run_projection directly: move one row, add an unrelated new row, confirm the run doesn't raise, only the new row writes, the moved row's old projection is untouched, and report['skipped'] names it) plus test_listener_skips_org_change_and_leaves_old_projection_untouched (real-DB write-time-listener test: project, then change organization_id via the ORM, commit, confirm no raise and the projected row's organization_id is still the original). (3) R2-3: business_capabilities.py's 'code': target.code or f\"BC-{target.id}\" changed to 'target.code if target.code is not None else f\"BC-{target.id}\"', matching the CLI's COALESCE(bc.code, ...) semantics exactly instead of also substituting on code=''. Confirmed latent (no current writer sets code='') but fixed as a one-token change. (4) R2-4: tests/smoke/test_capability_journey.py's _unified_capabilities_count skip-on-unparseable-200-body replaced with pytest.fail (including both response bodies in the message) -- a 200 with no discoverable count is now a hard failure, not a skip. (5) R2-5: reworded CLAUDE.md's store-agreement paragraph to state explicitly that the ratchet's measured value is database-dependent (a fresh CI DB measures 0, the shared local dev/test DB with its 87 unclassified rows measures 1, as one example, not THE current state), and stopped stating either run's number as if it settles anything; task 03's result.md given a Round 3 section making the same correction for its own 'measured 0 this run' line rather than editing that historical entry in place. Verification this round: pytest tests/test_capability_projection.py -q -> 17 passed (15 + 2 new); pytest tests/test_tenant_isolation.py -q -> 18 passed; pytest tests/smoke/test_capability_journey.py -q -> 1 passed; ruff check on every touched Python file -> clean; bash -n on deploy-schema.sh -> clean; python scripts/verify.py --tag static -> 48 passed, 0 failed, 1 pre-existing skip (css-build, no local Tailwind CLI); python scripts/verify.py --gate store-agreement -> passed, measured 0 this run (this specific run's number, not asserted as fixed -- see R2-5 fix above). Not committed, per instructions -- left for refuter re-review."
}