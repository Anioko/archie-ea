{
  "from": "builder",
  "to": "refuter",
  "artifact": "docs/buckets/sap-s4-interface-register/tasks/03-plateau-gap-provisioning.result.md",
  "approval_status": "approved",
  "gate_conditions": [
    "d3_interface_gaps_excluded_from_capability_gap_surfaces",
    "d3_architecture_id_stamped",
    "d4_null_gap_type_no_longer_500s",
    "d5_comparison_routes_in_authorisation_matrix",
    "d6_bare_verify_run_reported_honestly",
    "d3_round3_unified_gap_register_and_roadmap_stats_fixed"
  ],
  "unmet_conditions": [],
  "gate_conditions_met": [
    "d3_interface_gaps_excluded_from_capability_gap_surfaces: grep -rn \"Gap.query\" app/ run and every hit evaluated, not just the three sites named in the brief. Added a Gap.gap_kind != 'plateau_transition' exclusion (or an equivalent shared base-query filter) to: app/routes/unified_enterprise_routes.py (strategic_planning_dashboard, risk_assessment, gap_analysis -- the canonical S-11 register, ai_architecture_analysis, enterprise_dashboard), app/modules/architecture/services/roadmap_generator.py (_get_gaps_to_process), app/modules/architecture/services/gap_archimate_service.py (get_gaps_for_roadmap), app/main/routes_archimate_roadmap.py (open_gaps summary), app/modules/solutions_strategic/v2/services/enterprise_posture_service.py (open/total/overdue/unowned gap counts, refactored onto one shared base query). Confirmed already-safe and left unchanged: app/main/routes_application_roadmap.py (scoped by application_component_id, which interface gaps never set), capabilities/routes/roadmap_routes.py's id-scoped .get()/.in_() lookups, ImplementationGap/ComplianceGap sites (different models), and comparison_routes.py itself (already correctly filters FOR plateau_transition, the inverse case, by design). New test tests/test_interface_gap_capability_gap_isolation.py seeds one real interface gap and one real capability gap in the same org/architecture and asserts each of the three named surfaces returns the capability gap and excludes the interface gap -- 3/3 pass.",
    "d3_architecture_id_stamped: interface_gap_service.raise_interface_gap now resolves TechnologyRoadmapInitiative.query.get(initiative_id).architecture_id and sets it on the created Gap (previously NULL despite being available at creation time). tests/test_interface_gap_service.py::test_raise_interface_gap_writes_a_valid_plateau_transition_gap gained an assertion gap.architecture_id == architecture.id -- passes.",
    "d4_null_gap_type_no_longer_500s: comparison.html line 97 changed to (gap.gap_type or '').replace('_', ' ').title() or '—' -- the repo's em-dash null convention, not a blank or fabricated label. Proved with a real Playwright browser test (tests/smoke/test_archetype_journeys.py::test_comparison_page_renders_a_gap_with_null_gap_type), not source-reading: writes a real Gap row with gap_type=None against a provisioned plateau pair, then drives solution_architect to the real rendered comparison page, asserts HTTP 200 (not 500), the gap's name is visible after reload, and the em-dash character appears in that row. Confirmed red (AttributeError: current_org_id, before adding g.current_org_id to the test's write) then green after. 1 passed.",
    "d5_comparison_routes_in_authorisation_matrix: added POLICY['/interface-register/comparison'] (same archetype set as the existing / and /new rows, same justification -- guard runs before initiative_id is read). Added a module-scoped seeded_interface_initiative fixture (real TechnologyRoadmapInitiative + ArchitectureModel) and test_interface_register_provision_comparison_authorisation, parametrized over all 11 archetypes, following the exact seeded-fixture pattern of the existing test_interface_register_edit_route_authorisation -- confirms the GET boundary via _observe, then (for allowed archetypes, when the provision form is still present -- the fixture is module-scoped and idempotent, so only the first allowed archetype actually provisions) submits the real POST with a CSRF token read off the rendered DOM (same pattern as tests/smoke/test_data_entity_lifecycle.py) and asserts it is not refused with 403. raise_gap (POST /<id>/gaps) intentionally not given a second parametrized test: it shares the identical _guard() call, already covered by the /comparison GET row and by the existing journey test that exercises that exact POST as solution_architect -- a second matrix test would add runtime, not coverage. tests/smoke/test_authorisation_matrix.py + tests/smoke/test_archetype_journeys.py together: 82 passed, 0 failed.",
    "d6_bare_verify_run_reported_honestly: ran bare python scripts/verify.py --require-db (TEST_DATABASE_URL and DATABASE_URL both pointed at the real local Postgres) to completion -- 53 passed, 3 failed, ~78 minutes. All 3 failures evaluated for pre-existing-ness with real evidence, not asserted: schema-drift FAILs on a typed_arb_constraints FK-validation warning already logged at boot before this round's edits existed and unrelated to Gap/interface_register (flask reconcile-schema --dry-run confirms 0 columns would add, only the pre-existing FK issue remains); tests FAILed on a hard timeout (TEST_SUITE_TIMEOUT_SECONDS=3600 in scripts/verify.py), not a failing assertion -- every test file this task is responsible for was run directly and separately and passed (21 + 7 + 82 = 110 passed across the targeted runs); nav-verified FAILs reading route_verification.json dated 10 Sep 2026 (6 days stale, predates this bucket), a direct downstream consequence of the tests gate never completing and regenerating it -- interface_register.index, one of the 16 it lists as untested, is directly and repeatedly exercised by tests/smoke/test_authorisation_matrix.py's POLICY['/interface-register/'] row. Did not run a full git-stash-isolated before/after pair (a single run already cost ~78 minutes with the tests gate alone consuming the full 3600s timeout) -- flagged in the result doc as available on request if refuter wants it despite the evidence above.",
    "d3_round3_unified_gap_register_and_roadmap_stats_fixed: refuter round 2 found D3's grep (\"Gap.query\" only) missed db.session.query(Gap) and raw 'FROM gaps' SQL, both present on a real ADR-0008 store-agreement surface (app/services/gap_register_service.get_unified_gap_register(), served by phase_e_routes.py:40 and phase_f_routes.py:158) that Round 2 never touched. Confirmed bug: _ROADMAP_GAP_TYPES and interface-register's own GAP_TYPES both use the literal 'retirement' for unrelated concepts, so an interface gap raised with gap_type='retirement' was returned TWICE from get_unified_gap_register() (unfiltered Source-2 query + Source-3 roadmap-type-matching query), and one raised with 'protocol_change'/'new_interface' leaked once, mislabelled Phase D. Fixed via Aider (--message-file, diff reviewed, file re-read after each run): app/services/gap_register_service.py -- added Gap.gap_kind != GAP_KIND_PLATEAU_TRANSITION (imported constant) to all 3 sites named in the finding PLUS a 4th (gap_summary_by_phase's implementation-severity aggregate, found by the exhaustive re-check, same bug shape). app/modules/solutions_strategic/v2/routes/roadmap_api.py -- added a composable _gap_kind_clause to both raw-SQL aggregates (by_priority, by_type) preserving the existing _org_where/_org_params tenant filtering unchanged, plus fixed the ImplementationGap.query.count() total sitting beside them (not named in the brief, same file/bug, left inconsistent otherwise). Ran the process-note-mandated exhaustive re-check across all 4 read forms (Gap.query, db.session.query(Gap), select(Gap), raw FROM gaps) across all of app/: every other hit is either already filtered (Round 2's 5 files + gap_archimate_service.py:551, confirmed) or is a single-record/id-scoped lookup or capability-specific-column-scoped query interface gaps never populate -- evaluated per-site, not assumed. New test tests/test_interface_gap_capability_gap_isolation.py::test_unified_gap_register_does_not_double_count_retirement_type_collision raises a real interface gap with gap_type='retirement', calls get_unified_gap_register(), and asserts it does not appear at all (not 'once') -- this is the reproduction case refuter said matters. 8/8 tests pass in that file + test_interface_gap_service.py. python scripts/verify.py --tag static: 48 passed, 0 failed, 0 skipped. No existing test covers roadmap_api.py's get_statistics or gap_summary_by_phase directly (grepped, zero hits) so none needed re-running beyond the above."
  ],
  "notes": "Round 2 (D3-D6, by builder): D1 and D2 (real bugs -- cross-initiative plateau-pair leak, provisioning idempotency race) were already fixed by the coordinator directly in commit 1b44ace4 before that round started; not touched again there. Round 2 covers D3-D6, described above. Round 3 (this update) covers a single reopened finding: refuter's round-2 review found D3's original grep pattern was too narrow and missed get_unified_gap_register()/roadmap_api.py's raw SQL. Fixed via Aider per the coordinator's explicit instruction for this round (both edits applied cleanly on the first attempt; a later litellm.NotFoundError from a free-tier summarizer model surfaced only AFTER the real edit had already been applied and confirmed via re-read, so it did not affect the result). approval_status is pending, not approved -- that determination belongs to refuter, per this bucket's established pattern. Left uncommitted per instruction.",
  "timestamp": "2026-09-16T02:10:00Z"
}
