"""Programme type templates (ADR 0012): reviewed data files instantiated
once into ordinary programme records. See ``loader.py`` for the catalogue,
``validator.py`` for the shape/content rules, ``hashing.py`` for the shared
canonical-hash function, and ``labels.py`` for plain-language names (US-16).
"""
